from bpllib import get_dataset
from bpllib.utils import easy_datasets, medium_datasets, hard_datasets

print("% autogenerated by dataset_table.py")
print("""
\\begin{table}[h]\\centering

\\small
\\begin{tabular}{ l c c c c }
\\hline
\\textbf{Dataset} & \\textbf{\\#instances} & \\textbf{\\#feat.} & \\textbf{$\\oplus$ class}  \\\\
\\hline
""")

for datasets in [easy_datasets, medium_datasets, hard_datasets]:
    for dataset in datasets:
        X, y, positive_label, target_feature = get_dataset(dataset, return_extra_info=True, remove_inconsistent=True)
        n_features = X.shape[1]
        n_instances = X.shape[0]
        print(
            f'\\texttt{{{dataset}}} & {n_instances} & {n_features} & {str(target_feature)[:8]}={str(positive_label)[:10]} \\\\')
    print('\\hline')

print("""
\\end{tabular}
\\caption{\\label{tab:datasets} Benchmark datasets for the rule learning task.}
\\end{table}
""")
